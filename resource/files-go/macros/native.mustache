import React, { Component } from 'react';


import {WNativeGuideBook as guidebook} from 'uhutu-ts/dist/wnative/index';

import {
  TouchableOpacity,
  StyleSheet,
  Text,
  Image,
  ListView,
  FlatList,
  ScrollView,
  TextInput,
  Dimensions,
  RefreshControl,
  Switch,
  View
} from 'react-native';

{{#out.config.styleUrl}}
import styles from "{{{out.config.styleUrl}}}";
{{/out.config.styleUrl}}

import styletg from "../../agent/theme/global-style";




export default class  extends Component {


  static navigationOptions = {
    title: '{{{out.config.pageTitle}}}',
  };


  constructor(props) {
    super(props);
    
    this.state = {{{out.state}}};
  }





  {{#out.templates}}
    x_template_render_{{name}}(item){
      return (
      {{{content}}}
      )
    }
  {{/out.templates}}



  

  render(){

    const { navigate } = this.props.navigation;

    return (
      {{{out.content}}}
    )
  }

};
